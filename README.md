# Crucible Agent

Crucible is an alpha-stage, local-first engineering protocol for Hermes Agent. It turns rough software requests into durable decisions, gated implementation, review, and fresh verification evidence.

The project is under active development on the v0.1.0 release train. It has no telemetry and no hosted-service dependency.
